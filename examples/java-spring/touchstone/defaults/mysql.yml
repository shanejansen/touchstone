---
databases:
  - name: myapp
    statements:
      - >
        CREATE TABLE users (
          id INT AUTO_INCREMENT,
          first_name VARCHAR(255),
          last_name VARCHAR(255),
          email VARCHAR(255),
          PRIMARY KEY (id)
        )
      - "INSERT INTO users VALUES (1, 'John', 'Smith', 'john.smith@example.com')"
